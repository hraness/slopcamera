# Portfolio surface acceptance

`verify:portfolio` selects `portfolio-surfaces-v3`. It certifies the Catppuccin
system default, quiet documentation, contour marketing field, shared interactive
hero light and sculpted surfaces. It does not relabel historical migration,
Lantern, refinement, support or workflow-example evidence.

The complete expanded authored homepage has a 40,100-byte ceiling. The current
predecessor measures 39,495 bytes; palette/pattern attributes and spacing add 70
bytes, and the shared inert hero adds 444 bytes, giving 40,009 bytes. All of those
bytes remain counted. Exact-ceiling, UTF-8 overflow and unknown-slot rejection
tests remain required. The separate complete built HTML measures 65,272 bytes
against a 65,600-byte ceiling: predecessor 64,732 plus palette attributes 61,
compiled header classes 26 and shared inert hero markup 453. The complete
sealed document stays counted, with 328 bytes of headroom.

The complete theme bundle has a 31,800-byte ceiling. Its two theme colors are
resolved from the immutable shared palette by the static builder, so the browser
does not receive the unused 6,263-byte palette table. The predecessor measures
28,561 bytes; the shared hero controller adds 2,906, giving 31,467 with 333 bytes
of headroom. Exact-boundary and UTF-8 overflow tests remain required. Build-time
constants change neither the colors nor the browser behavior.

The two complete emitted stylesheets have a 451,100-byte combined ceiling. The
exact predecessor build is 347,562 bytes; the immutable shared palette, material,
typography and hero rollout adds 100,572 bytes to its foundation and 2,532 bytes
to its finalized recipe union. The current total is 450,666 bytes, leaving 434
bytes of headroom. Every byte remains counted, including repeated declarations
across distinct cascade layers. Exact-boundary, either-artifact and UTF-8
overflow rejection remain required. This budget change does not alter any
native comparison tolerance, the 133 cases, or the historical profiles.

Run it with an explicit independent, clean predecessor checkout and a reviewed
schema-seven manifest:

```sh
bun run verify:portfolio --baseline-directory /absolute/predecessor/apps/web --baseline-manifest /absolute/reviewed-baseline.json
```

The predecessor revision is `cec02b4ea84b7e30f35a1e66224cbe1bb1066446`, tree
`938b7d4a09924a199f0a45234de7f74effbdafda`. Both source and built artifacts are
hash-bound. Its material snapshot remains exactly
`0e089bc18f9a0409f0e74b1fb7192f468956e386`; the portfolio verifier explicitly
selects that retained revision only after checking the baseline's Git identity,
then repeats the same selection during final byte revalidation. Ordinary builds
and candidate snapshots continue to require the current material revision.
The candidate must be clean, use one immutable design-kit revision
for its preset and material, and retain the pinned Node 24 / Playwright 1.62.0
Chromium lifecycle. Use the repository's existing exclusive browser admission.

This predecessor includes the premiere/compute scenes, revised homepage gallery,
and corrected hosted Credits, platform and release facts. Their content stays exactly paired instead of being exempted from the
semantic comparison. The earlier v1/72a0f21 profile is retained byte-for-byte in
`site-portfolio-profile-v1.ts`; its original source is also retained at
`52060fa37088a181b535851f6ceafa389db36935`. No native acceptance was claimed for
that prepared profile. The prepared v2/a742b29 profile is retained byte-for-byte in
`site-portfolio-profile-v2.ts`; its source is retained at
`9e91a08`. Neither prepared profile received native acceptance.
Never relabel earlier manifests or receipts as v3.
The supplemental spatial-guide review is specified in
`PORTFOLIO_CURRENT_MAIN_REVIEW.md` and remains required before delivery.

The gate retains all 76 ordinary cases, eight native copy cases, 40 documentation
cases and nine real player cases. The ordinary matrix retains its 720-second
bound inside the full 1,200-second bound. CSP, local network custody, native focus,
appearance, RTL, coarse pointers, forced colors, responsive geometry, stylesheet
disable/restoration, native disclosure, copy failures and the real success timer,
media policy and positive process/server collection remain required. The
200-percent cases remain reflow-equivalent evidence, not native browser zoom.

Meaningful body markup and interaction semantics are paired with the predecessor.
The only new semantic island is a counted, strictly inert two-layer backdrop.
Compiler classes and the three finite compiled copy-state transports may change;
their authored hooks, content and semantics remain paired. Literal palette,
gradient, typography, shadow and backdrop expectations are independent of target
custom properties. Every measured current geometry and paint value is also
compared with the complete independently reviewed render reference, including
documentation prose/code and all copy states. Only admitted local asset origins
are normalized; unknown resources fail. The reference uses lossless interning to
keep repeated style records readable and bounded without omitting measurements.

The canonical reference is `scripts/portfolio-design-reference.json`. It binds
the predecessor revision, immutable design-kit revision, named independent review
and all 133 cases. Player timing is validated by real probes; its visual reference
slots are explicitly `null`. The native receipt binds the reference bytes, profile
and oracle source hashes along with the full source/artifact closure.

When the reference does not exist, the worker may retain
`site-portfolio-UNREVIEWED-reference.json` in its task-owned evidence directory
after all positive probes and collection succeed. That run fails acceptance and
never publishes a successful result. Inspect the captured data and representative
native renders independently, correct any design defects, and author the reviewed
canonical reference only after the immutable package candidate is fixed. Reserved
capture/pending/unreviewed markers are rejected. A malformed existing reference
fails before browser work. A capture, screenshot or previously passing receipt
cannot substitute for a fresh current-tree acceptance run.

# Current-main spatial guide qualification

This is a pending native qualification plan, not an acceptance receipt. It is
additional to all 133 `portfolio-surfaces-v3` cases, their existing bounds and
the independent capture/review/rerun process. Do not change historical profiles,
skip semantic comparison, or infer success from source inspection.

The independent predecessor is commit
`cec02b4ea84b7e30f35a1e66224cbe1bb1066446`, tree
`938b7d4a09924a199f0a45234de7f74effbdafda`. Before admission, independently review
its clean source, frozen dependency graph and actual generated artifact manifest.
Preserve the old 72a0f21 manifest and profile separately. Both sides now contain
the exact same authored gallery content; only the reviewed design changes differ.

The current homepage figure order is `native-product`, `premiere-wall`,
`island-pulse`, `interference-field`, `native-fluid`, `compute-temple`. The last
two replace the previous character/education examples. Existing 133-case checks
already compare this literal order, native focus and media policies. Review the
4:3 fluid example together with the gallery's new `min(52vh, 24rem)` media-height
cap: preserve its intrinsic aspect ratio, keep captions readable, and check
keyboard hit targets without stretching or clipping the frame.

The existing 40 documentation cases do not visit `/docs/how-to/direct-scenes`.
Add a separately identified, bounded 12-case supplement under the same owned
native browser/network/process protocol before delivery:

- Six direct visits: 320, 390 and 1440 CSS pixels, each light and dark.
- Two no-JavaScript visits: 390 pixels, light and dark.
- Two forced-color visits: 390 pixels, light and dark.
- Two manual playback cases at 1440 pixels: `premiere-scene` and `compute-temple`.

For the ten document visits require actual viewport width, no horizontal page
overflow, one visible h1 before the fold, local fonts, visible native focus,
unique IDs, correct disclosure/sidebar behavior, readable code and captions,
and strict current/predecessor semantic equality. Measure prose, headings,
code/pre overflow, figure/video geometry and links. Require the exact eight
figure IDs in order: `camera-orbit`, `camera-dolly`, `camera-crane`, `camera-rail`,
`camera-tripod`, `camera-handheld`, `premiere-scene`, `compute-temple`.
Each docs video remains paused with controls, `preload="none"`, no preview
attribute, and no MP4 request before interaction. No-JS cases still expose the
manual player and useful source/guide links. Compare all new design observations
to a separately reviewed exact reference after the immutable package build.

For each manual case use a real trusted interaction with the named local video;
require its registry-bound URL, duration/dimensions, successful decoded playback,
advancing currentTime, native pause and zero unrelated requests/errors. Do not
render new scenes, invoke an authoring tool, or contact a provider. Keep existing
reduced-motion, Save-Data, visibility, denied playback, failed media, captions,
CSP and cleanup negative controls in the mandatory main gate.

Do not install, build or capture before the verified UI/design-kit patch pair
and disk/browser admission. This plan does not authorize a larger byte ceiling:
measure the final ordinary/preview graphs and independently review any actual
budget mismatch. The new literal opacity expectations and emitted texture
identity still await the immutable build; retain historical values until their
separately reviewed current-profile update.

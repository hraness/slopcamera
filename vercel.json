{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "echo 'slopcamera-api: functions only'",
  "functions": {
    "api/[[...path]].ts": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/$1" }
  ]
}

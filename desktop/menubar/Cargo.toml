[package]
name = "slopcamera-menubar"
version = "0.1.0"
description = "Slopcamera menu-bar companion for the agent outputs directory"
edition = "2021"
rust-version = "1.77.2"
license = "MIT"
publish = false

[build-dependencies]
tauri-build = { version = "=2.6.3", features = [] }

[dependencies]
desktop-foundation = { git = "https://github.com/hraness/desktop-foundation", tag = "v0.3.0" }
libc = "0.2"
tauri = { version = "=2.11.5", features = ["tray-icon"] }
